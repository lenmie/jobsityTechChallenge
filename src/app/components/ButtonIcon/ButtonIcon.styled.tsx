import styled from '@emotion/native';
import { flexbox, layout, space, color, border } from 'styled-system';

export const TouchableContainer = styled.TouchableOpacity`
  ${space}
  ${color}
  ${layout}
  ${border}
  ${flexbox}
`;
